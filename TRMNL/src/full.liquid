<!--
Home Assistant TRMNL sensors v0.1.0

- GitHub: https://github.com/bbronisz/trmnl_ha_sensors
- License: MIT License

Changelog:
- v0.1.0: Initial release - First version with basic sensors grouping functionality
-->

<style>
  .sensor-value {
    font-weight: 600 !important;
  }
  .weather-icon {
    position: absolute;
    top: 68%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 50px;
  }
</style>
<div class="mashup mashup--1Lx1R">
  <div class="layout">
    <div class="grid grid--cols-2">
      {% for entity in entities %}
        {% if entity.i and entity.i != blank %}
          {% assign entity_icon = entity.i | replace: ':', '-' %}
        {% else %}
          {% assign entity_icon = 'mdi-gauge' %}
          {% for icon_pair in icon_map %}
            {% assign icon_data = icon_pair | split: ':' %}
            {% if icon_data[0] == entity.device_class %}
              {% assign entity_icon = icon_data[1] %}
              {% break %}
            {% endif %}
          {% endfor %}
        {% endif %}
      <div class="item pb--8">
        <div class="content">
          <span class="label">
            <span class="mdi {{ entity_icon }}"></span>
            {{ entity.n }}
          </span>
          <span class="value value--tnums sensor-value" data-value-fit="true">
            {%- if entity.val == "unavailable" -%}
            -
            {%- else -%}
            {{- entity.val -}}{{- entity.u -}}
            {%- endif -%}
          </span>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</div>
